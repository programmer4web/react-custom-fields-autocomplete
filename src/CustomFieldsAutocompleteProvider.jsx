import React, { useState } from 'react';

// Unique ID generator
const generateId = () => Math.floor(Math.random() * 1000000).toString();

const CustomFieldsAutocompleteProvider = ({}) => {

}

export default CustomFieldsAutocompleteProvider;